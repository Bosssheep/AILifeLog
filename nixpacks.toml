[phases.setup]
nixPkgs = ["python3", "python311Packages.pip", "nodejs"]

[phases.install]
cmds = ["npm install", "python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
